export enum UserRole {
  CUSTOMER = 'customer',
  ADMIN = 'admin',
  OTHER = 'other',
}

export enum UserType {
  MALE = 'Mr.',
  FEMALE = 'Mrs.',
  MISS = 'Miss.',
}

export enum MartialStatusType {
  SINGLE = 'single',
  MARRIED = 'married',
}
